# Maia MCP Mendix Demo

This repository is used to demonstrate how Mendix Maia can use GitHub through the Model Context Protocol (MCP).

The repository defines a simple user management system that includes:

- a User entity
- API structure
- example data

Maia can read these files and generate Mendix artifacts such as:
- Domain Model
- Microflows
- Pages
